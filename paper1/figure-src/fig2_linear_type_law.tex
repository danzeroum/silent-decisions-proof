% fig2_linear_type_law.tex — Figure 3 (column width)
% Redesign 2026-09-15 (COMSI-2026-04-0112, design/ieee-figure-redesign):
% simplified to the token-flow + three rejected bypass attempts, dropping
% the earlier "three barriers" annotation layer (must_use nuance is fully
% covered by the adjacent prose in Sec. 3's "Affine, not strictly linear"
% paragraph, so this figure no longer restates it and cannot overclaim it).
% Formula direction is the corrected (E ⊗ C) ⊸ V throughout.
\documentclass[border=2pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,calc}
\usepackage{amssymb}
\usepackage{cmll}
\begin{document}
\begin{tikzpicture}[
  font=\footnotesize,
  tok/.style={draw, rounded corners=2pt, align=center, inner sep=4pt,
    minimum width=24mm, minimum height=11mm, fill=black!04},
  vnode/.style={draw, rounded corners=2pt, align=center, inner sep=4pt,
    minimum width=24mm, minimum height=11mm, fill=black!12},
  reject/.style={draw, rounded corners=1pt, align=center, inner sep=3pt,
    font=\scriptsize, fill=black!04, minimum width=24mm},
  arr/.style={-{Stealth[length=5pt]}, semithick},
  note/.style={align=center, font=\scriptsize, text=black!75}
]

% ── E and C, joined into the formula ─────────────────────────────────────
\node[tok] (E) at (0,0) {\textbf{EvidenceToken} $E$};
\node[tok, right=8mm of E] (C) {\textbf{ComplianceToken} $C$};
\node[font=\small, below=9mm of $(E.south)!0.5!(C.south)$] (law)
  {$(E \otimes C) \multimap V$};
\draw[semithick] (E.south) -- ++(0,-4mm) -| (law.north);
\draw[semithick] (C.south) -- ++(0,-4mm) -| (law.north);
\node[vnode, below=9mm of law] (V) {\textbf{Sealed Verdict} $V$};
\draw[arr] (law) -- (V);

% ── three rejected bypass attempts, as a compact side column ────────────
\node[reject, right=14mm of C, anchor=west] (r1) {Clone $\rightarrow$ rejected};
\node[reject, below=3mm of r1] (r2) {Forge $\rightarrow$ rejected};
\node[reject, below=3mm of r2] (r3) {Bypass constructor\\ $\rightarrow$ rejected};
\node[draw=black!45, rounded corners=3pt, inner sep=5pt,
  fit=(r1)(r2)(r3)] (box) {};
\node[font=\scriptsize\bfseries, align=center, text width=32mm,
  below=4mm of box.south] {only \texttt{Verdict::new} is well-typed};

\end{tikzpicture}
\end{document}
