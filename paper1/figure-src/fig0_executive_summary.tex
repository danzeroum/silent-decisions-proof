% fig0_executive_summary.tex — Figure 1 (figure*, full double-column width)
% Redesign 2026-09-15 (COMSI-2026-04-0112, design/ieee-figure-redesign):
% three-panel structural analogy, replacing the earlier three-band diagram.
% Inspired by the author's original fig1_silent_decision_analog.pdf layout
% (three columns, three stacked steps, one synthesis line) — recomposed with
% only the corrected claims: BTV's punchline is scoped to "cannot be
% constructed" within the type perimeter, never an unscoped "impossible."
\documentclass[border=2pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,calc}
\usepackage{amssymb}
\begin{document}
\begin{tikzpicture}[
  font=\footnotesize,
  flowbox/.style={draw, rounded corners=2pt, align=center, inner sep=5pt,
    minimum width=40mm, minimum height=11mm, fill=black!04},
  final/.style={draw, rounded corners=2pt, align=center, inner sep=5pt,
    minimum width=40mm, minimum height=11mm, fill=black!12},
  hdr/.style={font=\small\bfseries, align=center},
  arr/.style={-{Stealth[length=5pt]}, semithick},
  note/.style={align=center, font=\scriptsize, text=black!70}
]

% ── column x-positions ────────────────────────────────────────────────────
\def\colA{0}
\def\colB{48mm}
\def\colC{96mm}

% ── headers ──────────────────────────────────────────────────────────────
\node[hdr] (hA) at (\colA,0) {A. Memory safety in C};
\node[hdr] (hB) at (\colB,0) {B. Silent decision in AI};
\node[hdr] (hC) at (\colC,0) {C. BTV construction boundary};

% ── Panel A ──────────────────────────────────────────────────────────────
\node[flowbox, below=6mm of hA] (a1) {\texttt{free(ptr)}\\ dangling pointer};
\node[flowbox, below=9mm of a1] (a2) {\texttt{use(ptr)}\\ undefined behavior};
\node[final, below=9mm of a2] (a3) {crash discovered\\ years later};
\draw[arr] (a1) -- (a2);
\draw[arr] (a2) -- (a3);

% ── Panel B ──────────────────────────────────────────────────────────────
\node[flowbox, below=6mm of hB] (b1) {decision emitted\\ in milliseconds};
\node[flowbox, below=9mm of b1] (b2) {audit record\\ written asynchronously};
\node[final, below=9mm of b2] (b3) {record may be\\ lost before review};
\draw[arr] (b1) -- (b2);
\draw[arr] (b2) -- (b3);

% ── Panel C ──────────────────────────────────────────────────────────────
\node[flowbox, below=6mm of hC] (c1) {EvidenceToken +\\ ComplianceToken};
\node[flowbox, below=9mm of c1] (c2) {\texttt{Verdict::new(e, c)}\\ consumes both};
\node[final, below=9mm of c2] (c3) {without the required\\ tokens, a BTV \texttt{Verdict}\\ cannot be constructed};
\draw[arr] (c1) -- (c2);
\draw[arr] (c2) -- (c3);

% ── synthesis line ───────────────────────────────────────────────────────
\node[note, below=8mm of a3, anchor=north west, text width=136mm, align=center]
  at ($(a3.south)!0.5!(c3.south)+(0,-8mm)$)
  {The same architectural pattern: the obligation is separated from the act it must constrain.};

\end{tikzpicture}
\end{document}
