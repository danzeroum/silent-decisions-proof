target/
paper1/target/
paper2/target/
__pycache__/
*.pyc

# Raw sweep output from local runs of scripts/run_sweep.sh. Final CSVs cited
# in the paper must be explicitly committed with descriptive names.
/data/sweep_raw_*.csv

# Round 3 (Task B2/B3) committed sweep datasets — explicitly re-included:
# paper-cited raw data collected this round (sandbox + GitHub-hosted runner).
# The generic pattern above still ignores LOCAL run output; these two files
# are the committed evidence pair (CSV + environment fingerprint alongside).
!data/sweep_raw_20260914T171258Z.csv
!data/sweep_raw_20260914T164735Z_runnervmlun5p.csv

# OS-07 (COMSI-2026-04-0112, closes F7/F10): third environment, first run
# with the 5-mode grid (full_pipeline_durable + status_quo_digest_log).
# Reduced-footprint (BTV_SWEEP_TARGET_WALL_SECS=2, not the 90s headline
# target) — see data/sweep_env_20260914T224433Z.txt for the label.
!data/sweep_raw_20260914T224433Z.csv
